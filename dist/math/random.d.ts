/**
 * 生成一个min-max之间的随机数
 * @param min 最小值
 * @param max 最大值
 * @returns 返回一个min-max之间的随机数
 * @example
 * random(1, 5)
 * // => 3
 */
declare function random(min: number, max: number): number;
export default random;
